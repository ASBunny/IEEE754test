Документация для файла Representation of any number.

Программа позволяет выполнять преобразование числа к его битовому представлению и наоборот.
Программа реализована на языке Python в кодировке utf-8. Выполняется для версии Python 3.2 и выше.

Описание функций:
NumberToBits(float) - переводит число типа float в соответствующую строку из 32-х символов.
			                В качестве аргумента может принимать неопределенность (nan) и бесконечность (-inf, inf).
BitsToNumber(string) - переводит строку из 32-х символов в число типа float (реализация согласно стандарту IEEE 754);
BitsToNumberIn(string) - переводит строку из 32-х символов в число типа float (реализация встроенными средствами языка).

В конце программы приведены примеры использования функций.
